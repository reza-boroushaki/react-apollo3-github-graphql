export const __repo_owner = 'reactjs';
export const __repo_name = 'reactjs.org';
const __repo = `repo:${__repo_owner}/${__repo_name}`;

export const __default_type = 'is:issue';

export const __default_search = {
    searchText: __default_type,
    issueState: null,
    query: __repo + ' ' + __default_type
}